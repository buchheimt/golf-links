<div class="row">
  <div class="col-sm-8 mx-auto course-card">
    <a href="<%= course_path(course) %>">
      <h4><%= course.name %></h4>
      <p><%= course.location %></p>
    </a>
  </div>
</div>
