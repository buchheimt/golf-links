<div class="container">
  <div class="row">
    <div class="col-sm-5 mx-auto user-card">
      <h2>Edit <%= @course.name %></h2>
      <%= link_to "Delete Course", course_path(@course), method: "delete", class: "btn" %>
      <br>
      <%= render partial: 'form', locals: {course: @course} %>
    </div>
  </div>
</div>
