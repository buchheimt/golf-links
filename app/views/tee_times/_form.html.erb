<%= form_for [user, tee_time] do |f| %>

  <p>
    <%= f.label :course_id %>
    <%= f.text_field :course_id %>
  </p>

  <p>
    <%= f.label :time %>
    <%= f.text_field :time %>
  </p>

  <%= f.submit "Create Tee Time" %>
<% end %>
